This is a simple ai agent build from the serie [Building a minimal AI agent from scratch](https://minimal-agent.com/)

The twist is that was build using [vala](https://valadoc.org/) instead of python.

Maybe it could be completed some day, as a proof of concept it works as expected, watch out it is in YOLO mode!